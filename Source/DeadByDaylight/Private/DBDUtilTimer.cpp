#include "DBDUtilTimer.h"

void UDBDUtilTimer::Update(FDBDTimer& Timer, float deltaTime) {
}

void UDBDUtilTimer::Stop(FDBDTimer& Timer) {
}

void UDBDUtilTimer::ResetTo(FDBDTimer& Timer, float time) {
}

void UDBDUtilTimer::Reset(FDBDTimer& Timer) {
}

bool UDBDUtilTimer::IsDone(const FDBDTimer& Timer) {
    return false;
}

float UDBDUtilTimer::GetTimeLeft(const FDBDTimer& Timer) {
    return 0.0f;
}

float UDBDUtilTimer::GetTimeElapsed(const FDBDTimer& Timer) {
    return 0.0f;
}

float UDBDUtilTimer::GetStartTime(const FDBDTimer& Timer) {
    return 0.0f;
}

float UDBDUtilTimer::GetPercentTimeLeft(const FDBDTimer& Timer) {
    return 0.0f;
}

float UDBDUtilTimer::GetPercentTimeElapsed(const FDBDTimer& Timer) {
    return 0.0f;
}

UDBDUtilTimer::UDBDUtilTimer() {
}

